const getters = {

    // defaultXY: () => [104.07, 30.67], //默认地图中心。暂定为成都
    // MonthTotal: state => state.performanceCount.MonthTotal,
    userInfo: state => state.userInfo,
}

export default getters;